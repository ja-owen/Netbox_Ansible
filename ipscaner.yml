- name: "Test Netbox prefix module"
  connection: local
  hosts: localhost
  gather_facts: False
  collections:
    - netbox_community.ansible_modules
  vars:
    netbox_url: http://netboxldap
    netbox_token: 869e49848b645d3b903800ab2d7325725ce1fcb7
  tasks:
    - name: Create prefix within Netbox with only required information
      netbox_prefix:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        data:
          prefix: 192.168.5.0/24
        state: present

